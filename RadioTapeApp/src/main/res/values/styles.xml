<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <!--<item name="android:actionBarTabTextStyle">@style/AppTheme.ActionBar.TabText</item>-->


        <!--<item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionMenuTextColor">@color/text_tab_selected</item>-->

        <!-- http://developer.android.com/guide/topics/ui/actionbar.html#Style -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="android:actionMenuTextColor">@color/color_greenradiotape</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="actionMenuTextColor">@color/color_greenradiotape</item>
    </style>

    <!-- general styles for the action bar -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>
        <!--<item name="android:background">@android:color/black</item>-->

        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/TitleTextStyle</item>
        <!--<item name="background">@android:color/black</item>-->
        <!--<item name="backgroundStacked">@android:color/black</item>
        <item name="backgroundSplit">@android:color/black</item>-->
</style>

<!-- action bar title text -->
    <style name="TitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/color_greenradiotape</item>
    </style>

    <!-- action bar tab text -->
    <style name="TabTextStyle" parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@color/color_greenradiotape</item>
    </style>

    <!--<style name="AppTheme.ActionBar.TabText" parent="android:style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/text_tab_selected</item>
    </style>-->

    <!-- ActionBar styles -->
    <!--<style name="MyActionBar" parent="android:style/Widget.Holo.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
    </style>-->

    <!-- ActionBar title text -->
    <!--<style name="MyActionBarTitleText" parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/text_tab_selected</item>
    </style>-->

    <!-- ActionBar tabs text styles -->
    <!--<style name="MyActionBarTabText" parent="android:style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/text_tab_selected</item>
    </style>-->

</resources>
